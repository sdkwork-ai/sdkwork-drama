# SDKWork repository AI workspace

Local AI workspace metadata for this repository.

- `skills/`: repository-local agent skills.
- `plugins/`: repository-local agent plugins.

Related: `../sdkwork-specs/SDKWORK_WORKSPACE_SPEC.md`, `../sdkwork-specs/AGENTS_SPEC.md`.

Local ignore policy: `local/`, `tmp/`, `cache/`, `secrets/`, `manual-backups/` are never committed (see `.sdkwork/.gitignore`).
