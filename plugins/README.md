# plugins/

Purpose: repository-owned plugin assets for extendable surfaces of the `drama` application.

Owner: sdkwork-drama maintainers.

Allowed: plugin manifests and authored plugin code scoped to this repository.

Forbidden: third-party plugin binaries, secrets, generated artifacts.

Related specs: `../sdkwork-specs/SDKWORK_WORKSPACE_SPEC.md`.

Verification: `node ../sdkwork-specs/tools/audit-repository-baseline.mjs --root .`
